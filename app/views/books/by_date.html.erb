<%= render 'main' %>
<h5>Available from <span class="badge badge-secondary"><%= @start1.to_s(:long) %></span> to  <span class="badge badge-secondary"><%=@ends1.to_s(:long)%></span> </h5>

<table class=" table table-striped sortable">
  <thead>
    <tr>
      <th>Name</th>
      <th>Author name</th>
      <th>ISBN</th>
      <th>Actions</th>
    </tr>
  </thead>
<% @book_by_date.each do |book| %>
     <tr>
        <td><%= link_to book.name,book_path(book) %></td>
        <td><%= book.author %></td>
        <td><%= book.isbn %></td>
        <td>
          <% if current_user && current_user.admin? %>
            <%= link_to "Edit",edit_book_path(book), class: "btn btn-default btn-info" %>
            <%= link_to "Delete",book_path(book), data: {:confirm => 'Are you sure?'}, :method => :delete , class: "btn btn-default btn-danger" %>
          <% else %>
            <%= link_to "Rent",new_book_registration_path(book), class: "btn btn-default btn-primary" %>
          <% end %>
        </td>
    </tr>
<% end %>
</table>